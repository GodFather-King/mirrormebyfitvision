import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fitvision.mirrorme',
  appName: 'mirrormebyfitvision',
  webDir: 'dist'
};

export default config;
